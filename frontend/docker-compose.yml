version: "3.9"
services:
  frontend:
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
    ports:
      - "4200:4200"
    volumes:
      - .:/app
    extra_hosts:
      - "host.docker.internal:host-gateway"
networks:
  lct:
      driver: bridge